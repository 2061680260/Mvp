<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    android:text="WY"
    android:drawableTop="@drawable/wy_selector"

    <style name="radiostyle">
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@drawable/text_selector</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">12sp</item>
    </style>
    <style name="myprogress" parent="Base.Widget.AppCompat.ProgressBar">
        <item name="android:minWidth">80dp</item>
        <item name="android:maxWidth">80dp</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:maxHeight">80dp</item>
    </style>

    <style name="detail" parent="AlertDialog.AppCompat.Light">
        <!-- Customize your theme here. -->
        <!--标题栏（actionBar）颜色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--&lt;!&ndash;状态栏颜色&ndash;&gt;-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>


</resources>
